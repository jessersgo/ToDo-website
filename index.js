

const input = getElementById("task");
const tasksList = getElementById("list"); 

addTask = () =>{
    //first check it has something added/ not an empty string
    //since js must use === for strict equality
    if(input.value === ''){
        alert("Please type in a task to add :)!")
    } else{
        let
    }

}